apiVersion: v1
kind: Service
metadata:
  name: {{ include "validator.redisName" . }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ include "validator.redisName" . }}
  ports:
    - port: 6379
      targetPort: 6379
